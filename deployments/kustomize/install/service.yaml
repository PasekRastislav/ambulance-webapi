kind: Service
apiVersion: v1
metadata:
  name: rpasek-ambulance-webapi
spec:
  selector:
    pod: rpasek-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port